<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.dancefun20">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application

        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".LoopviewActivity" />
        <activity android:name=".FunmenuActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".SignActivity" />
        <activity android:name=".AddLogActivity" />
        <activity android:name=".Userinfo_Activity" />
        <activity android:name=".LoglistActivity" />
        <activity android:name=".AriticleListActivity" />
        <activity android:name=".MyfavorActivity"></activity>
    </application>

</manifest>